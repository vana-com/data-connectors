{
  "name": "iCloud Notes",
  "version": "1.2.0",
  "scope": "icloud_notes.notes",
  "dialect": "json",
  "description": "Notes exported from iCloud. Field shape reflects the data the CG-in-prod iCloud Notes script emits: one object per note with recordName, title, snippet, folder, pin state, created/modified dates, attachment flag, and extracted text content.",
  "schema": {
    "type": "object",
    "properties": {
      "notes": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "recordName":     { "type": "string" },
            "title":          { "type": ["string", "null"] },
            "snippet":        { "type": ["string", "null"] },
            "folder":         { "type": ["string", "null"] },
            "isPinned":       { "type": "boolean" },
            "createdDate":    { "type": ["string", "null"], "format": "date-time" },
            "modifiedDate":   { "type": ["string", "null"], "format": "date-time" },
            "hasAttachments": { "type": "boolean" },
            "textContent":    { "type": ["string", "null"] }
          },
          "required": ["recordName", "title"],
          "additionalProperties": true
        }
      },
      "total":    { "type": "number" },
      "userName": { "type": ["string", "null"] }
    },
    "required": ["notes"],
    "additionalProperties": true
  }
}
